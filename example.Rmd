---
title: "example"
output: html_document
---

```{r}
# "massarin/learnrhash" fixes "Column label not found" error
# devtools::install_github("massarin/learnrhash")

# Test it
example = readRDS(system.file("example.rds", package="learnrhash"))
learnrhash::extract_hash(example)
```
```{r}
quiz_example <- read.csv("quiz_example.csv")
learnrhash::extract_hash(quiz_example, hash = "learnrhash")
```
